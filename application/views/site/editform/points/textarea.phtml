<?= $label ?><br/>
<textarea
    name="<?= $fieldname ?>"
    placeholder="<?= htmlspecialchars($placeholder) ?>"
    <?php if ($required) printf('data-required="%s"', htmlspecialchars($required) ); ?>
><?php if ($default)  print htmlspecialchars($default); ?></textarea><br/>
